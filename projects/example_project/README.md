# Example project

Copy this directory to create a project manifest. Replace every example value,
then register the new manifest in `registry/projects.yaml` and run:

```bash
.venv/bin/jdgf validate
```

Templates are not registered as active projects.
