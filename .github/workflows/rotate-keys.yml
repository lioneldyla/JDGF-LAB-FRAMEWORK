name: Rotate API Keys

on:
  schedule:
    - cron: '0 3 1 * *'  # 1er du mois
  workflow_dispatch:

jobs:
  rotate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Check keys expiration
      run: echo "Vérification des clés API - Mise à jour manuelle recommandée dans GitHub Secrets"
